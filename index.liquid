---
layout: default.liquid
---

{% for post in collections.posts.pages %}
<h1><a href="/{{ post.permalink }}">{{ post.title }}</a></h1>
<span class="published">Published {{ post.published_date | date: "%b %d, %Y" }}</span>
{{ post.excerpt }}
{% endfor %}
